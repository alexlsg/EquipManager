<Window x:Class="UITest.FrmTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UITest"
        mc:Ignorable="d"
        Title="FrmTest" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Button Click="Button_Click" Width="75">开始</Button>
        <Button Click="Button_Click1" Width="75" Margin="465,0,260,0">暂停</Button>
        <RichTextBox Grid.Row="1" Name="rtb">
            <RichTextBox.Document>
                <FlowDocument LineHeight="1"></FlowDocument>
            </RichTextBox.Document>
        </RichTextBox>
    </Grid>
</Window>
